version: '3.8'
services:
  oracle-web:
    build: .
    container_name: oracle-web
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - GMAIL_USER=${GMAIL_USER}
      - GMAIL_PASS=${GMAIL_PASS}
    restart: unless-stopped
