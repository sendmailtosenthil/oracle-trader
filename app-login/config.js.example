// Copy this file to `config.js` and set your Oracle login password.
// `config.js` is gitignored so your real password is never committed.
self.ORACLE_CREDS = { user: "senthil", pass: "YOUR_PASSWORD" };
