venv/
__pycache__/
*.pyc
*.db
# Database backups (contain live broker enctoken + password hashes — never commit)
*.bak
oracle.db.*
.env
.DS_Store
.pytest_cache/
.coverage
htmlcov/
# Downloader: market data output + Google Drive OAuth secrets (never commit)
data/
credentials.json
token.json
