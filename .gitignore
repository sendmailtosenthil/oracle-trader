venv/
__pycache__/
*.pyc
*.db
# WAL sidecars — recreated by SQLite, and the -wal holds uncheckpointed rows
*.db-wal
*.db-shm
# Database backups (contain live broker enctoken + password hashes — never commit)
*.bak
oracle.db.*
.env
.DS_Store
.pytest_cache/
.coverage
htmlcov/
# Downloader: market data output + Google Drive OAuth secrets (never commit)
data/
credentials.json
token.json
# Bare-metal deploy: job logs
logs/
# Chrome extension local credentials (real password/secret — never commit)
extensions/oracle-login/config.js
extensions/zerodha-login/config.js
